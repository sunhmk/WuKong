org.eclipse.jetty is used in under spark 2.0
but hbase uses  org.mortbay.jetty.
Eclipse is the most recent home。

https://en.wikipedia.org/wiki/Jetty_(web_server)#History

scala:
servlet
1:static servlet-for resources
2：Redirect servlet-for redirect to other url
3：proxy servlet-for proxy handler


scalastyle-maven-plugin 
config file =>scalastyle-config.xml
check scala code style
for example:
importing file is grouped
class name start with Upper letter;
file ends with newline;
annotation must start with a space;

copy dependcy to ../lib
mvn dependency:copy-dependencies -DoutputDirectory=.../wukong/lib/  -DincludeScope=runtime
../bin/start-jetty
 